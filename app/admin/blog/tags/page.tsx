'use client';
import { useEffect, useState } from 'react';
export default function TagsPage(){ const [rows,setRows]=useState<any[]>([]); const [name,setName]=useState(''); const [slug,setSlug]=useState(''); const token=typeof window!=='undefined'?localStorage.getItem('noosheh-admin-token')||'':''; const load=()=>fetch('/api/admin/blog/tags',{headers:{Authorization:`Bearer ${token}`}}).then(r=>r.json()).then(setRows); useEffect(()=>{load();},[]); const add=async()=>{await fetch('/api/admin/blog/tags',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({name,slug})}); setName('');setSlug('');load();}; return <div className='admin-page' dir='rtl'><h1 className='admin-title'>Tags</h1><div className='admin-card p-4 flex gap-2'><input className='admin-input' value={name} onChange={e=>setName(e.target.value)} placeholder='name'/><input className='admin-input' value={slug} onChange={e=>setSlug(e.target.value)} placeholder='slug'/><button className='admin-btn primary' onClick={add}>add</button></div><div className='admin-card p-4'>{rows.map(r=><div key={r.id}>{r.name}</div>)}</div></div>}
