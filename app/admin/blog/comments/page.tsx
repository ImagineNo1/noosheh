'use client';
import { useEffect, useState } from 'react';
export default function CommentsPage(){ const [rows,setRows]=useState<any[]>([]); const [status,setStatus]=useState('pending'); const token=typeof window!=='undefined'?localStorage.getItem('noosheh-admin-token')||'':''; const load=()=>fetch(`/api/admin/blog/comments?status=${status}`,{headers:{Authorization:`Bearer ${token}`}}).then(r=>r.json()).then(setRows); useEffect(()=>{load();},[status]); const setSt=async(id:string,s:string)=>{await fetch('/api/admin/blog/comments',{method:'PATCH',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify({id,status:s})}); load();}; return <div className='admin-page' dir='rtl'><h1 className='admin-title'>Comments</h1><div className='admin-card p-4'><select className='admin-input' value={status} onChange={e=>setStatus(e.target.value)}><option>pending</option><option>approved</option><option>rejected</option><option>spam</option><option>trash</option></select></div><div className='admin-card p-4'>{rows.map(r=><div key={r.id} className='mb-3 border-b pb-2'><div>{r.author_name} - {r.content}</div><div className='flex gap-1'><button className='admin-btn' onClick={()=>setSt(r.id,'approved')}>approve</button><button className='admin-btn' onClick={()=>setSt(r.id,'rejected')}>reject</button><button className='admin-btn' onClick={()=>setSt(r.id,'spam')}>spam</button><button className='admin-btn' onClick={()=>setSt(r.id,'trash')}>trash</button></div></div>)}</div></div>}
